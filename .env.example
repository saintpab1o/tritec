# TRITEC Who's Who - Environment Variables
# Local dev: only ADMIN_PASSWORD is needed. Data uses local JSON files.
# Production (Vercel): KV and Blob vars are auto-set when you connect stores.

# Admin password for the /admin dashboard
ADMIN_PASSWORD=tritec2026

# --- Auto-set by Vercel when you connect KV store ---
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
# KV_URL=

# --- Auto-set by Vercel when you connect Blob store ---
# BLOB_READ_WRITE_TOKEN=
